//! atlas-model — Transformer architecture: MultiHeadAttn, FFN, RMSNorm, RoPE. OLMo 3 config. Stage 2.
//!
//! # Status: STUB — Stage 2 implementation pending.
//!
//! See CHARTER.md for the full build order and porting table.

#![allow(dead_code, unused_variables)]

/// Placeholder — implementation begins in Stage 2.
pub struct AtlasModel;

#[cfg(test)]
mod tests {
    #[test]
    fn placeholder_passes() {
        // Placeholder test — remove when real implementation starts.
        assert!(true);
    }
}
